{%- assign spec = content.spec -%}
{%- assign metadata = content.metadata -%}
{%- assign status = content.status -%}

{
  "properties": {
    "spec": {{ spec }},
    "metadata": {{ metadata }},
    "status": {{ status }}
  }
}