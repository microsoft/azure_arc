.DS_Store
.vscode
backup/
*tfstate*
.terraform
*.log
.vagrant
*.pem
.terraform.lock.hcl
azure_arc_data_jumpstart/gke/terraform/account.json
azure_arc_data_jumpstart/gke/terraform/artifacts/azure_arc.ps1
azure_arc_data_jumpstart/gke/terraform/artifacts/password_reset.ps1
azure_arc_data_jumpstart/eks/terraform/artifacts/azure_arc.ps1
azure_arc_data_jumpstart/eks/terraform/artifacts/password_reset.ps1
azure_arc_data_jumpstart/eks/terraform/config
azure_arc_data_jumpstart/eks/terraform/configmap.yml

# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sentitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
#
*.tfvars

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
#
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

infra.out
